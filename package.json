{
  "name": "ai-chat-app",
  "version": "1.0.0",
  "private": true,
  "description": "AI Chat client-side app packaged as an Android APK via Capacitor.",
  "scripts": {
    "sync": "cap sync android",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
